# Front-end (Streamlit Community Cloud) dependencies only.
# The engine's dependencies live in requirements-pipeline.txt and are installed
# by the GitHub Action, not here. The clustering bench (cluster_lab.py) needs
# scikit-learn, hdbscan and umap-learn, installed locally when needed.
streamlit>=1.38.0
pandas>=2.2.0
requests>=2.31.0
